# Public site URL used for canonical links, Open Graph and sitemap.
# Set this in your deployment environment (e.g. Vercel project settings).
NEXT_PUBLIC_SITE_URL=https://dockcentra.com

# --- Quote form delivery (not connected yet, reserved for a later stage) ---
# When a delivery method is connected, configure it here. Never commit real values.
# QUOTE_WEBHOOK_URL=
# QUOTE_NOTIFICATION_EMAIL=
